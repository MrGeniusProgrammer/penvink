import { TableIcon as LucideTableIcon } from "lucide-react";
import type React from "react";

export function TableIcon(props: React.ComponentProps<typeof LucideTableIcon>) {
  return <LucideTableIcon {...props} />;
}
